Para rodar o código, coloque 

javac -d bin $(find src -name "*.java")
java -cp bin App

"Ao tentar subir até o topo da cidade, você encontra uma criatura peculiar.
O camino demora anos, mas isso não de impedirá de terminá-lo. Não será uma
simples criatura que o parará"

mudanças:

implementação de efeitos:

com o padrão Observer, os efeitos (veneno e regeneração)
foram codados

duas cartas nova foram feitas (veneno e regeneraçao)

